name: Bootstrap OIDC

# Run this ONCE, manually, to create the OIDC trust anchor that Deploy then uses
# forever. It is the only step that needs pre-existing AWS credentials, because
# the deploy role cannot create itself before it exists (OIDC chicken-and-egg).
#
# Setup (all in the GitHub UI — no local tooling):
#   1. Settings → Secrets and variables → Actions → New repository secret:
#        AWS_BOOTSTRAP_ACCESS_KEY_ID       (an admin/bootstrap IAM user's key)
#        AWS_BOOTSTRAP_SECRET_ACCESS_KEY
#   2. Settings → Secrets and variables → Actions → Variables → New variable:
#        AWS_REGION        e.g. us-east-1
#        ECR_REPOSITORY    e.g. agentcore-solution1   (the agent_name)
#   3. Actions tab → "Bootstrap OIDC" → Run workflow.
#   4. After it succeeds, DELETE the two bootstrap secrets. Deploy uses OIDC now.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (temporary keys)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_BOOTSTRAP_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_BOOTSTRAP_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.15.4

      # Apply ONLY the deploy role + its policies. -target is appropriate here:
      # this is a deliberate partial apply to lay the trust anchor; the next full
      # deploy.yml run reconciles everything else. The OIDC provider already
      # exists account-wide and is consumed via a data source, so it isn't
      # targeted (nothing to create).
      - name: Create deploy role
        working-directory: infra
        run: |
          terraform init
          terraform apply -auto-approve \
            -target=aws_iam_role.github_deploy \
            -target=aws_iam_role_policy_attachment.github_deploy_poweruser \
            -target=aws_iam_role_policy.github_deploy_iam \
            -target=aws_iam_role_policy.github_deploy_tfstate

      # Write the role ARN into Actions vars so deploy.yml is self-configuring —
      # nothing to copy-paste by hand.
      - name: Publish deploy role ARN to Actions vars
        working-directory: infra
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ROLE_ARN=$(terraform output -raw github_deploy_role_arn)
          gh variable set AWS_ROLE_ARN --body "$ROLE_ARN"
          echo "Set AWS_ROLE_ARN=$ROLE_ARN"
          echo "Bootstrap complete. Delete the AWS_BOOTSTRAP_* secrets now."
