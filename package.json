{
  "name": "agentcore-solution1",
  "version": "0.1.0",
  "description": "AI agent deployed on Amazon Bedrock AgentCore Runtime",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/app.js",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.4.0"
  }
}
